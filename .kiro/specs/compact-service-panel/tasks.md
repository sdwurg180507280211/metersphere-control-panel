# 任务

- [x] 抽出两行服务卡片和摘要逻辑，保留状态与操作语义。
- [x] 将完整诊断移入窗口内侧边面板。
- [x] 限制服务区高度，提供收起功能，联动日志高亮。
- [x] 补充测试和使用说明，完成自动测试、构建和隔离界面验证。

## 验证记录

- 前端 30 项（新增 9 项组件 SSR / 摘要 / 开发模式详情面板回归）、后端 67 项、可靠性 193 项全部通过。
- `verify:desktop` 和生产构建通过；无新增依赖。
- 浏览器使用 `node scripts/tests/reliability-ui-fixture.cjs --services` 的隔离数据，12 个服务含长错误、健康、启动中、停止、未知状态；不连接真实后端或用户配置。
- 1440×900、1280×800、1024×700、800×600：卡片均 72px，日志约占工作区 60%，无页面横向溢出。小窗口的日志筛选和搜索自动换行，不与操作按钮重叠。
- 1280×800：日志面板约 431px；收起服务区约 672px；展开后服务列表滚动位置保留。
- 卡片日志按钮、日志下拉框及控制面板日志切换高亮正确；详情关闭返回触发按钮，从详情进入日志时焦点移至日志区；打开详情不改变日志面板尺寸。
- 模拟断线后观测显示“待刷新”，旧错误标记为“上次异常”，不再显示绿色健康确认。
- 隔离验证未发送服务启停、SQL 请求；浏览器正常连接状态无控制台错误或警告。
