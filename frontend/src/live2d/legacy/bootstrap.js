// Legacy Live2D engine bootstrap
// This file serves as a fallback entry point for the old implementation
// Only used when WAIFU_FEATURE_FLAGS.engine === 'legacy'

export function initLegacyEngine() {
  console.log('Legacy Live2D engine is active')
  // Legacy initialization logic remains in index.html for now
  // This file exists to provide a clear migration boundary
}
